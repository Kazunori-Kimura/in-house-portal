import { Typography } from '@mui/material';
import Layout from '../../components/Layout';

const Enquete: React.FC = () => {
    return (
        <Layout>
            <Typography variant="h1">Enquete</Typography>
        </Layout>
    );
};

export default Enquete;
